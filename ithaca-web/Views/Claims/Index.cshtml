@using TodoListClient.Models
@model IdentityModel;

@{
   ViewData["Title"] = "Current Claims";
}

<h1>
    Your current claims @Model.Data.Count
</h1>

<table class="table table-bordered table-striped">


    @foreach (var item in Model.Data)
            {
            <tr>
                <td>@item.Key</td>
                <td>@item.Value</td>
            </tr>
        }

   
</table>

